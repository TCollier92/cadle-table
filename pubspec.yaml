name: cadle_table_server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.4 <3.0.0'


dependencies:
  args: ^2.0.0
  html: ^0.15.0
  http: ^0.13.4
  shelf: ^1.1.0
  shelf_cors_headers: ^0.1.2
  shelf_router: ^1.0.0
  shelf_virtual_directory: ^3.0.0

dev_dependencies:
  lints: ^1.0.0
  test_process: ^2.0.0
  test: ^1.15.0
